{% extends 'admin/base.html.twig' %}

{% block body %}


            <div class="col-md-8 offset-md-1">
                <h4 class="mt-5 my-4 text-muted">Modifier un Tableau</h4>
                {{ form_start(form)}}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-secondary">Modifier</button>
                {{ form_end(form) }}
            </div>


{% endblock %}