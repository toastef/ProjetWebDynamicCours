{% extends 'base.html.twig' %}
{% block body %}
<div style="width: 100%; height: 100%; display: flex; flex-direction: row; justify-content: center; text-align: center">

    <h3 style="width: 30%;">{{ title }}</h3>
    <hr>

    <p> Tableau ajouté par : {{ firstName|capitalize }} {{ lastName|capitalize  }}</p>
    <div style="width: 650px; height: auto; border-radius: 7px; text-align: center; box-shadow: rgba(0,0,0,0.89) 0 2px 4px, rgba(0,0,0,0.6) 0 7px 13px -3px, rgba(0,0,0,0.56) 0 -3px 0 inset;">
        <img style="width: 100%; height: auto;"
             src="{{ email.image('@image/' ~ paint) }}" alt="image de l'oeuvre">
    </div>
    <p>Au prix de : {{ prix|format_currency('EUR', locale='fr') }}</p>
</div>
{% endblock %}




